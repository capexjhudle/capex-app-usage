<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!--
      Para sa in-app prompt na nag-e-exempt sa app sa battery optimization
      (Doze / App Standby). Kapag hindi naka-exempt, inaantala o kinakansela
      ng system ang mga alarm natin — dito nasisira ang hourly cycle.

      BABALA: pinagbabawalan ito ng Play Store maliban sa iilang kategorya
      ng app. Sideload/internal distribution lang dapat ang app na ito.
    -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"
        tools:ignore="BatteryLife" />

    <!--
      Exact alarms sa Android 13+ (API 33 pataas), auto-granted — hindi na
      kailangang pumayag pa ang user. Kung wala ito, `setAndAllowWhileIdle`
      ang gagamitin ng background-fetch library at pwedeng ma-late ng ilang
      minuto sa :00 ang hourly run kapag naka-Doze.

      Ang API 31–32 ay saklaw na ng SCHEDULE_EXACT_ALARM na idinadagdag ng
      library mismo (auto-granted din sa mga bersyong iyon).

      BABALA: kailangan ng Play Store declaration para dito. Sideload lang.
    -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!--
      Explicit na package visibility para sa mga app na sinusubaybayan natin.
      Backup ito kung sakaling matanggal ang QUERY_ALL_PACKAGES (mahigpit ang
      Play Store dito) — sa listahang ito, gagana pa rin ang getPackageInfo().
    -->
    <queries>
        <package android:name="com.cis3mobileapp.app" />
        <package android:name="com.viber.voip" />
        <package android:name="com.android.chrome" />
        <package android:name="com.google.android.apps.maps" />
        <package android:name="com.waze" />
    </queries>

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
